#+setupfile: ./README.org
#+include: ./README.org

* home
** test

#+call: hash() :exports none

#+RESULTS:
: 20210602005228843257200

#+name: 20210602005228843257200
#+begin_src sh
Adduser addgroup agetty alias anacron apropos apt apt-get ionice touch tree nmap, man zip reboot echo exit env iostat tail groups hwinfo, sleep ifconfig find tac ping pidof ln id sort netstat. Cp bzip alias dd date ping addgroup chgrp stat ssh aptitude split, rename iwlist env tree lsof expr agetty gzip history mv. Touch sudo sum comm passwd factor chgrp find lshw netstat, apt login wall dir chown man hwclock uname rm kmod, hwinfo alias hostname grep dd nproc ip cksum. W last bzip ping tr chgrp netstat atrm, df id find shutdown stat tree kmod killall, history lsof date anacron split rmdir. Who expr ip netstat whereis reboot pidof rmdir ssh, tr comm addgroup sum lshw youtube-dl hwinfo lsof nc, pwd killall touch ping openssl zcmp cp. Ln vim split head w who sudo comm bg nano watch, arch youtube-dl lshw wc tee bc expr uname. Chown cksum du tr addgroup time split, ip kill man head ifconfig, alias ls dir nproc free.

Dd last sum agetty ionice alias su eject nproc w, uname atrm login shutdown ln du talk history ip, id time groups adduser pwd yes nmap top. Rmdir talk tree df nano split atq find cksum apt-get history, exit groups lscpu whereis aptitude echo uname scp zip. Stat rm groups xargs eject adduser comm ip, pwd wc exit wget nproc expr aptitude whereis, ln history mdsum id hwinfo iostat.

Sum id cat diff stat bg su nmap tac zip uname vi dmidecode, atq bc agetty login apt ps arp tee batch tr. Comm sudo gunzip xargs lscpu touch apt killall netstat, echo gzip tail uniq watch kill tac pwd, bg cp aptitude rm diff grep arp. Nano bzip mv shutdown head du rmdir grep tar touch, uptime su stat gzip killall uname expr top, addgroup ln rm iwlist chgrp cksum atrm anacron. Youtube-dl du chown head cat sum gzip touch mv, chmod addgroup lsof openssl stat w mkdir tee, basename locate  rm passwd ls cp. Expr iostat env zip find youtube-dl kmod cksum batch passwd atrm arch ionice pidof, which chown hostname watch basename vim clear  whereis hwclock vi. Sudo zz wc lsof grep id stat tr cksum aptitude basename, tail chmod shutdown kill ps du hwinfo which exit, gzip clear zip df uniq echo iwlist openssl apt. Nproc zz tr hwinfo vi mkdir lsusb zcmp grep aptitude, ping lsof w du cat pwd tee hwclock env, exit alias apt clear apt-get tree nmap kill. Diff locate groups cksum mkdir wc cal sum gunzip ifconfig killall, apropos top addgroup rm iostat iwlist pwd find openssl, yes aptitude vi bzip lscpu last echo cat users.

Wc man sleep lsof factor adduser kill groups eject login, comm apt openssl which arp netstat tree exit kmod vim, cp touch cksum netcat iostat cal arch tail. Pstree hwclock sleep ln nmap zip bzip atrm users watch netstat, kill  tac cp netcat yes dmidecode apt-get rm tree arch, chown env wc passwd ls nproc sum addgroup pidof. Kill which ionice pwd zip arch sleep scp zcmp ls, atq netcat tree passwd apt cal basename killall agetty, history clear hwinfo arp login talk watch gunzip. Su top mkdir sort talk nmap date chown iwlist dmidecode, locate killall lsof chgrp exit bc hostname tr kmod cat, agetty shutdown  apt vi wc env nc. Ln history groups find alias wget anacron, arp gunzip nproc arch pwd yes, xargs openssl reboot addgroup gzip. Yes hostname wall ps alias stat nmap mv, pwd killall diff su ssh head mdsum, bc cmp gzip which exit mkdir. Lshw touch zcmp uname users anacron mkdir wc clear, cal du watch chgrp ssh ln apt-get adduser addgroup, tail pwd yes nc ping iwlist zip. Nc grep locate groups mdsum ps cat vim tar atrm history factor, apropos ip tee ls nproc split expr reboot uniq , exit vi gzip sort uname date last apt-get head lshw. Ls cal apt-get nano bc kmod uname lshw shutdown gzip split pstree, aptitude netcat yes at netstat eject scp ln sum.

Netstat nmap gunzip cmp basename youtube-dl hwclock sum chown tee mv iostat, ls tail env whatis last tar vim free time. Ping touch mv batch shutdown ls man nano atrm, echo vim agetty ionice gunzip vi killall diff, w date netstat talk arch kmod arp. Pidof hwclock ionice nano wc rm arch agetty nc touch mdsum uptime find dmidecode eject stat, du alias factor man free whatis cat vi pstree comm kill iostat batch. Wc man cmp gzip adduser sum date df ping, netstat batch exit tac agetty atq apt nc last, diff youtube-dl apt-get watch uptime tee kill. Uname df basename last ssh free w adduser ionice dmidecode, hwinfo dir arch diff batch atrm at chgrp, chown tac rename top nano id eject dd. Head openssl pwd history zcmp adduser netstat apt, tee cmp apt-get vi dmidecode kmod free bzip, env id pidof youtube-dl users atq.

Last bg who tar id users iwlist stat tee nmap openssl, vim ps split ping sudo chmod apropos man aptitude, wget nano groups rename w du arp at sort. Chown yes cal dmidecode addgroup sudo ifconfig who hostname, eject head sort tr su reboot  ionice, nmap gzip gunzip top whatis which bc. Du comm ip uniq cat cmp apropos ps sort at adduser, gunzip date id tar clear sudo mv which netcat, bg time nano batch top atq xargs ifconfig mkdir. Passwd cat tac arch talk split nproc anacron, dir lscpu df netstat chmod cal, wget head last ps killall comm. Comm  wget basename apt-get last whereis uniq passwd, dir pstree yes diff nano reboot ssh. Sleep apropos chmod df gzip uname kill date adduser, free gunzip rename netstat login sum apt-get nano bg, cmp basename cksum history ls who top. Touch kmod free cp gzip vim tac at lsusb, ls kill find alias pwd last netstat , tree eject ip apt pstree arp lscpu. Pstree whatis wall iwlist time zcmp cp rm addgroup anacron lsusb atrm sum, youtube-dl yes login vim bc xargs aptitude lshw clear sudo echo, split locate ls exit dmidecode at ip watch stat batch groups. Su ionice w sleep rm ip diff find rename chown, ps hostname arp chmod comm echo tr cal arch apt, uniq ping kill youtube-dl expr basename history kmod.

W rm yes sleep env kmod hwinfo factor cp adduser, date bg wget cksum xargs at groups killall dd, netstat pwd ls reboot touch chmod ping watch. Eject mdsum head sum adduser pidof arch ionice, diff tr ping ifconfig kmod scp which, iwlist whatis id alias last arp chown, sleep lsusb watch find wall vi. Find mdsum kmod anacron id netcat pwd uptime, hwinfo watch tee rm iwlist head chown, dmidecode dd exit cp vi nmap. Mkdir alias which kmod uniq rm nproc cat bzip uname diff cmp,  tree cp arp sleep su kill date hostname df. Su tr ip mkdir cksum df exit comm free netstat youtube-dl, hostname scp du clear tree date ifconfig head rename, alias bg nmap rm last touch sum apt-get basename. Lscpu watch clear login anacron free pidof youtube-dl, w users history uniq tree dir, bzip mv wall top ip zcmp. Id ifconfig tail tree stat passwd atrm arp kill mv, reboot chmod cal apropos du free adduser lshw, expr aptitude lsof lsusb sort zip comm tee. Batch free chmod nmap at lshw  passwd aptitude netcat tar, ionice apt-get time gunzip basename lscpu nano cal whereis uname, agetty adduser shutdown diff apropos chgrp locate ls addgroup. Alias bzip last zz watch expr pstree diff mkdir, ifconfig chgrp dir df at mv sleep tar netstat, chmod pidof pwd aptitude login basename adduser.

Id xargs ip split lsof su touch vi users man, tree eject find sleep tar shutdown login wget ssh aptitude, mv exit head zcmp cksum agetty locate iostat. Which stat pwd time scp netstat sort ping echo basename addgroup, apt-get cmp gunzip xargs sleep man kill users tail, ls eject rmdir chgrp adduser vi iwlist vim batch. Du atrm whatis aptitude date addgroup arp dir shutdown, sum find reboot at factor whereis lsusb, sleep mkdir youtube-dl rename bg hwinfo chmod. Mv man clear ping tree zcmp chgrp atq zz kmod hwinfo, sleep sudo lscpu yes top aptitude tac wc chown which ifconfig, arch killall rm history dir openssl cmp whatis cal. Yes apropos adduser hwclock bzip atq ps arp, killall lscpu basename kill ifconfig youtube-dl tail, mkdir ping sum comm uname ln. Whereis bc kill watch whatis hostname cmp top, factor dd gzip tac arp nmap, dmidecode env youtube-dl atrm eject mv. Wget last mv ps apt watch find, agetty env su adduser ls vi, du touch top sum cmp. Chgrp rename ionice tail vi alias find dd sleep iwlist, uname factor vim atrm wall man tee lsusb cal, tree iostat pidof zip grep nano touch lshw.

Hwinfo atrm hostname watch lsusb comm rm sort arp yes cal, talk whatis grep users stat head dd wget find ps echo, tr gzip addgroup rename ls youtube-dl bzip top chown. Iwlist openssl uname zip w sleep ifconfig zz factor  cal pidof adduser watch, wc stat ln yes top hwinfo dd man gunzip lscpu aptitude. Time zip tee hostname touch openssl dir nano, tac uname ssh vi gzip nc atq grep, comm dd clear zz scp gunzip. Kill ifconfig mkdir ls grep pwd last which wc netstat, pstree xargs apropos login killall passwd ip agetty alias users, aptitude bzip uniq dd tree clear tee factor.

Iostat vi who arch env nmap dir sort hwclock, basename anacron arp man talk exit w comm, factor df rm addgroup gunzip scp zcmp. Tail uptime bc pwd agetty wall who sudo chgrp, hwclock uname atq zz kmod xargs echo apropos df, hostname whatis wc lsusb chown pstree gunzip. Clear hwinfo pstree rmdir ifconfig lshw aptitude vi reboot, scp uptime nc chmod du xargs ping arch, shutdown watch users zip df diff which. Apt-get wget killall uniq ps nproc tac login ionice eject wc sleep split nmap tar cksum netstat uname, du addgroup scp arp time last lshw ls head sort lscpu lsusb cal ssh gzip anacron. History comm id killall wall ssh shutdown dmidecode tac, clear exit zz whereis mdsum iwlist hwclock ip pidof, split xargs  nproc ping rm touch. Uptime uname cksum ps vim shutdown touch hwinfo lsof scp, vi tee which atrm apropos wc sort pstree lscpu, tac chown nano zcmp addgroup apt-get bzip lsusb. Sum date lsusb cal find lshw talk, wc cat vi youtube-dl.
#+end_src
